eureka:
  client:
    service-url:
      defaultZone: http://eurekaServer:8761/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true
spring:
  application:
    name: notifications